{
"id": "1c0de02a",
"title": "Resolve transcript tool review findings",
"tags": [
"tui",
"tools",
"correctness"
],
"status": "closed",
"created_at": "2026-07-16T20:18:02.033Z"
}

Resolved all six transcript tool review findings.

- `agent_end` now transitions every residual nonterminal invocation to `aborted`, preserving skipped sequential calls through a coalesced draw.
- Committed tool-result records retain their message position; live eviction detaches and reparents the same embedded root when its assistant leaves the 200-message window.
- `TranscriptView` now admits at most 64 tool IDs across embedded, standalone, and committed placement. Excess calls/results receive bounded omission rows.
- Tool display projection bounds full-output paths, notice count, each notice, and notice scanning before OpenTUI wrapping.
- `ToolCallView` clears native selection before shrinking/destroying selectable preview rows; transcript reset does the same.
- One transcript-owned renderer live request refreshes elapsed labels only for visible running tools, skips unchanged tenths, and is released at terminal state/disposal.
- Updated ADR 0013, architecture, and performance constraints.

Validation: `bun run check` passes (format, lint, typecheck, 7 script tests, 139 coding-agent tests, 127 TUI tests, and 20 CLI tests).
