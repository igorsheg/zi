{
"id": "1c128e7e",
"title": "Integrate session resource discovery",
"tags": [
"coding-agent",
"resources",
"skills",
"prompts",
"tui",
"parity"
],
"status": "closed",
"created_at": "2026-07-16T06:28:33.527Z"
}

Implement the agreed core session-resource slice: immutable discovery snapshots, Pi-compatible global/project skills and prompt templates, bounded YAML/frontmatter parsing, AgentSession expansion/catalog ownership, terminal command aggregation, diagnostics, tests, and documentation updates. Reference pi-mono at pinned 0e6909f0.

Implemented and verified:

- Concrete stateless `ResourceLoader.load()` returns deeply frozen `SessionResources`.
- Bounded context/system/append, Pi-compatible core skill and prompt discovery, YAML frontmatter, ignore rules, deterministic project precedence, canonical dedupe, diagnostics, and fixed limits.
- `AgentSession` owns the snapshot, composes skill metadata into the system prompt, exposes catalogs/commands/diagnostics, and expands direct/steering/follow-up resource input.
- Mode-owned `InteractiveCommands` aggregates built-ins with the current session and keeps resource dispatch in `AgentSession`.
- Updated glossary, ADR, architecture, TUI architecture, and parity roadmap.
- `bun run check` passes (coding-agent 114, TUI 98, CLI 18, scripts 7).
