{
"id": "27c1d1b0",
"title": "Keep built-in tool rows semantic",
"tags": [
"tui",
"coding-agent",
"tools",
"bug"
],
"status": "closed",
"created_at": "2026-07-18T05:23:16.614Z"
}

Fixed the valid-output trigger: SessionShell counted newline-terminated output differently from the shared truncation contract, so validators rejected normal Bash/task details and projectors selected generic JSON. Shell line counts now match truncation semantics, with real Bash/task-output regression coverage. Also made every known built-in projector total: malformed/obsolete details degrade inside the same semantic row; only unknown tools use JSON generic presentation. Updated ADRs/specs and transcript expectations. `bun run check` and `git diff --check` pass.
