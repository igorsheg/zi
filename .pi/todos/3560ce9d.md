{
"id": "3560ce9d",
"title": "Resolve semantic tool presentation review findings",
"tags": [
"coding-agent",
"tui",
"tools",
"correctness"
],
"status": "closed",
"created_at": "2026-07-18T10:47:30.605Z"
}

Resolved all four review findings with one authoritative path per concern.

- Write/Edit now treat resolved `writeFile()` as the mutation commit point. Cancellation is checked before mutation, but no post-commit abort can report a committed file as cancelled. Deterministic regressions prove committed writes/edits return success while pre-commit aborts remain cancellation.
- Added shared `splitTextLines()` / `countTextLines()` semantics: one terminal LF terminates the final usable line rather than adding an empty split sentinel; intentional preceding blank lines remain. SessionShell tracks whether its stream ends in LF, truncation and Write use the shared count, Read uses the shared split, and the TUI consumes the same split contract. Exact terminated 2,000-line Bash and truncation regressions pass.
- Edit bounds each oversized diff line to an explicit added/removed/context omission line before aggregate truncation. Successful details must contain at least one displayed `+` or `-` line, so header-only truncated persisted details are rejected. Coding-agent and native diff fixtures cover a 60 KiB changed line.
- Hidden body policies now short-circuit before splitting/wrapping. Visible terminal, source, text, and diff bodies parse bounded logical data then wrap only admitted head/tail/edge rows. New cell wrappers retain bounded head/tail rows even for one 100,000-character logical line. Stable roots and 12/200 row caps remain intact.
- Updated implementation and TUI performance specs, line-count fixtures, complete-turn/visual fixtures, and omission wording.

Validation: `bun run check` passes (166 coding-agent, 141 TUI, 20 CLI, 7 script tests); `git diff --check` and `bun run build` pass; `dist/zi` rebuilt.
