{
"id": "449c78b8",
"title": "Build lifecycle-stable transcript tool items",
"tags": [
"tui",
"transcript",
"tools",
"performance",
"parity"
],
"status": "closed",
"created_at": "2026-07-16T14:49:02.600Z"
}

Implemented against pi-mono 0e6909f0 and Grok Build c68e39f.

- `InteractiveStore` now consumes `message_update.assistantMessageEvent.contentIndex` and owns bounded `preparing | ready | running | done | failed | aborted` invocation transitions.
- Assistant thinking, prose, and tools render in source order. Streaming user/assistant roots promote at their admitted message index; each `ToolCallView` keeps one native root through args, execution, partial output, and committed result.
- Added framework-neutral coding-agent `projectToolDisplay()` for bash/read/write/edit/generic semantics, structured read/bash notices, bounded write/edit previews, compatibility edit args, and bounded executed edit diffs.
- Tool previews truncate after cell-aware wrapping, reconcile changed native lines, retain at most 200 expanded visual rows, and toggle in place through mode-owned `app.tools.expand` (Ctrl+O).
- Added lifecycle, real faux-provider argument delta, source-order, root identity, abort, sibling stability, narrow-width, structured notice, expansion, restore, and visual parity coverage.
- Added ADR 0013 and updated architecture, parity, performance specification, and engineering guidance.

Validation: full `bun run check` passes (format, lint, typecheck, scripts, coding-agent, TUI, and CLI tests).
