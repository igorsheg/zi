{
"id": "6554ad3d",
"title": "Align global agent directory with Pi",
"tags": [
"coding-agent",
"paths",
"resources",
"parity"
],
"status": "closed",
"created_at": "2026-07-16T07:14:39.602Z"
}

Change the default global root from $HOME/.zi to $HOME/.zi/agent while preserving exact <cwd>/.zi project configuration and full ZI_AGENT_DIR override semantics. Update tests and documentation.

Implemented `globalAgentDirName = "agent"`; default `getDefaultAgentDir()` now resolves `$HOME/.zi/agent`. Exact project `<cwd>/.zi` and complete `ZI_AGENT_DIR` overrides remain unchanged. Updated path tests, README, glossary, AGENTS rules, architecture, ADR 0011, and parity roadmap. Full `bun run check` passes.
