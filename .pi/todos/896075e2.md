{
"id": "896075e2",
"title": "Implement composer-owned login and logout workflows",
"tags": [
"tui",
"auth",
"picker",
"composer",
"tdd"
],
"status": "closed",
"created_at": "2026-07-15T12:06:54.051Z"
}

Implemented coding-agent `/login` and `/logout` descriptors plus closed terminal intents. PromptStore now owns provider/method/select/logout picker workflows, provider prompt settlement, operation identity, notices, Escape cancellation, and stale-session rejection. The always-mounted composer handles text/manual/secret input; secret mode uses native HIDDEN attributes, disables selection, bypasses Nano Store/transcript values, and clears before continuation. OAuth URL/device events render bounded OSC 8 links and route through an InteractiveMode-owned browser opener with process/time bounds and disposal. Agent/title updates, stored-only logout guidance, session replacement, and terminal-first auth shutdown are covered by real OpenTUI tests. Full check passed before the final bounded-browser-owner refinement; focused typecheck/lint/auth and shutdown tests pass afterward.
