{
"id": "b2931ff1",
"title": "Fix semantic tool presentation review findings",
"tags": [
"coding-agent",
"tui",
"tools",
"correctness"
],
"status": "closed",
"created_at": "2026-07-17T12:16:43.347Z"
}

Addressed all six accepted review findings:

- typed Bash timeout/capacity admission rejection plus schema-level empty-command rejection;
- authoritative active-tool source reuse with a structural `toolProjections` diagnostic;
- bounded generic JSON projection and incremental bounded result text collection;
- expanded preview direction preservation and no stale expand hint;
- relational detail validation and lifecycle/outcome matching;
- schema-valid edit batches now fit the aggregate UTF-8 argument bound.

Added focused coding-agent and TUI tests. `bun run check` and `git diff --check` pass.
