{
"id": "d21c2d10",
"title": "Reframe TUI around an interactive-mode owner",
"tags": [
"tui",
"architecture"
],
"status": "closed",
"created_at": "2026-07-14T21:04:34.683Z"
}

Removed the composer-picker implementation/spec/tests while preserving sound coding-agent model-selection APIs.

Established `InteractiveMode` as the prompt-submission owner, moved product UI into `packages/tui/src/modes/interactive/`, and added domain-free `Composer` and `PickerList` components under `packages/tui/src/components/`. Documented source layout, concrete interfaces, command ownership, and state ownership in ADR 0005 and `docs/tui-architecture.md`. Restored the frontend picker roadmap item to incomplete.

Validation: full `bun run check` passes.
