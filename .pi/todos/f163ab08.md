{
"id": "f163ab08",
"title": "Stop Bun standalone TreeSitter crash",
"tags": [
"tui",
"bun",
"crash",
"dogfood"
],
"status": "closed",
"created_at": "2026-07-18T05:46:49.948Z"
}

Reproduced the standalone failure mechanism: OpenTUI resolves `parser.worker.ts` under `/$bunfs/root`, which Bun 1.3 single-file compilation does not embed. Every Markdown highlight retried a missing Worker; a 500-iteration probe reproduced the churn matching the dogfood crash counters. Compiled OpenZi now marks the standalone runtime and TranscriptView injects an instance-scoped workerless TreeSitter client, retaining normal Markdown and plain selectable fenced code without retries. Development source keeps full OpenTUI highlighting. Added source and compiled-marker regression tests, documented the trade-off, passed `bun run check`/`git diff --check`, and rebuilt `dist/openzi`.
