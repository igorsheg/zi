# DOOM extension example

A helper-backed framebuffer extension demo.

Build the helper from this example directory:

```sh
cd examples/extensions/doom/helper
zig build -Doptimize=ReleaseSafe
```

Then load `examples/extensions/doom/doom.lua` and run:

```text
/doom
```
