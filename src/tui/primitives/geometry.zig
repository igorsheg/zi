pub const Size = struct {
    width: u32 = 0,
    height: u32 = 0,
};

pub const Point = struct {
    x: u32 = 0,
    y: u32 = 0,
};

pub const Rect = struct {
    x: u32 = 0,
    y: u32 = 0,
    width: u32 = 0,
    height: u32 = 0,
};

pub const Insets = struct {
    top: u32 = 0,
    right: u32 = 0,
    bottom: u32 = 0,
    left: u32 = 0,
};
